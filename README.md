# gpu-job-packing-opt
Multi-tenant GPU scheduler that packs multiple small jobs onto single GPUs.

A container-based HPC job scheduler with ML-powered resource prediction and intelligent GPU allocation.
